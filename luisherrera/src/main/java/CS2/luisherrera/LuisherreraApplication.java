package CS2.luisherrera;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for the Spring Boot project.
 * This class serves as the entry point for the application,
 * and it contains the main method to run the Spring application context.
 */
@SpringBootApplication
public class LuisHerreraApplication {

    public static void main(String[] args) {
        // The static run() method starts the Spring application.
        SpringApplication.run(LuisHerreraApplication.class, args);
    }
}
